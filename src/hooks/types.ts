export type card = {
  logo: any;
  number: number;
  numText?: string;
  text: string;
  progress: string;
};

export interface details  {
  logo: any;
  number: number;
  text: string;
};

export type modalDet = {
  logo:any;
  name:string;
  text:string;
}